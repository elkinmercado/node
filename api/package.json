{
  "name": "my-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx tsc server.ts",
    "watch": "tsc -b -w --clean server.ts",
    "start:api": "node server.js",
    "dev": "npm run build && npm run start:api"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}